import { getAirportDepartureOptions } from '../../../shared/airportDepartures.mjs';

export const findAirportDepartureOptions = (service, input, options) =>
	getAirportDepartureOptions(service, input, options);
